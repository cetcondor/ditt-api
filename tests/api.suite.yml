actor: ApiTester
modules:
    enabled:
        - \Helper\Api
        - Asserts
        - Symfony:
            app_path: 'src'
            var_path: 'var'
            environment: 'test'
        - Doctrine2:
            depends: Symfony
            cleanup: true
        - REST:
            depends: Symfony
