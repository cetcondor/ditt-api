<html lang="de">
    <head>
        <meta charset="utf-8" />
        <style>
            body {
                font-family: helvetica, roboto, arial, sans-serif;
            }
        </style>
    </head>

    <body>
        <h1>Krankmeldung</h1>
        <p>
            <strong>Daten: </strong><br>
            <ul>
                {% for workLog in workLogs %}
                    <li>{{ workLog.date | date('d.m.Y') }}</li>
                {% endfor %}
            </ul>
            <strong>Betrifft: </strong>{{ workLogs[0].workMonth.user.firstName }} {{ workLogs[0].workMonth.user.lastName }}<br>
        <strong>Variante: </strong>
        {% if workLogs[0].variant == 'SICK_CHILD' %}Kind krank{% endif %}
        {% if workLogs[0].variant == 'WITH_NOTE' %}Mit Krankenschein{% endif %}
        {% if workLogs[0].variant == 'WITHOUT_NOTE' %}Ohne Krankenschein{% endif %}
        <br>
        </p>
    </body>
</html>
