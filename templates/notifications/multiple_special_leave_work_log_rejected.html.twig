<html lang="de">
    <head>
        <meta charset="utf-8" />
        <style>
            body {
                font-family: helvetica, roboto, arial, sans-serif;
            }
        </style>
    </head>

    <body>
        <h1>Antrag auf mehrtägigen Sonderurlaub abgelehnt</h1>
        <p>
            <strong>Daten: </strong><br>
            <ul>
                {% for workLog in workLogs %}
                    <li>{{ workLog.date | date('d.m.Y') }}</li>
                {% endfor %}
            </ul>
            <strong>Betrifft: </strong>{{ workLogs[0].workMonth.user.firstName }} {{ workLogs[0].workMonth.user.lastName }}<br>
            {% if supervisor is not null %}
                <strong>Abgelehnt durch: </strong>{{ supervisor.firstName }} {{ supervisor.lastName }}<br>
            {% endif %}
            <strong>Begründung: </strong>{{ workLogs[0].rejectionMessage }}
        </p>
    </body>
</html>
